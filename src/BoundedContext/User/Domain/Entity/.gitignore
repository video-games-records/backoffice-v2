# This directory contains Domain Entities
# Core business objects with identity and lifecycle
# Entities encapsulate business logic and maintain state
# Example: User, Group, RefreshToken, SecurityEvent, etc.
# They are the heart of the domain model

# Keep this directory in git
!.gitignore