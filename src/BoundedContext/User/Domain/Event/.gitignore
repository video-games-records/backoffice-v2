# This directory contains Domain Events
# Events represent something important that happened in the domain
# They enable loose coupling between bounded contexts
# Example: EmailChangedEvent, PasswordChangedEvent, UserCreatedEvent, etc.
# Used for eventual consistency and integration patterns

# Keep this directory in git
!.gitignore