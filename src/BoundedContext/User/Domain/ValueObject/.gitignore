# This directory contains Value Objects
# Immutable objects that represent descriptive aspects of the domain
# They have no identity, only value equality matters
# Example: Email, UserId, Password, Address, etc.
# They encapsulate validation logic and business rules for values

# Keep this directory in git
!.gitignore