# This directory contains Doctrine-specific implementations
# Doctrine entity listeners, event subscribers, custom mappings
# Example: UserEntityListener, custom DBAL types, etc.
# Bridge between Doctrine ORM and Domain layer

# Keep this directory in git
!.gitignore