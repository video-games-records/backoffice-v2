# This directory contains Persistence layer implementations
# Repository implementations, data access objects, query builders
# Implements Domain Repository interfaces using specific technologies
# Example: Doctrine implementations, custom query optimizations, etc.

# Keep this directory in git
!.gitignore