# This directory contains Security Infrastructure
# Security adapters, user providers, authentication handlers
# Example: UserProvider, TokenGenerator, PasswordEncoder adapters, etc.
# Bridges between Symfony Security component and User domain

# Keep this directory in git
!.gitignore