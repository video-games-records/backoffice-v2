# This directory contains Custom Validators
# Symfony validation constraints specific to User context
# Example: UniqueEmailValidator, StrongPasswordValidator, etc.

# Keep this directory in git
!.gitignore